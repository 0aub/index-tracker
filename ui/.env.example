# ==========================================
# Frontend Environment Variables (Vite)
# ==========================================
# Copy this file to .env for local development
# Note: Variables must be prefixed with VITE_ to be exposed to the client

# ==========================================
# Mock Authentication (Development Only)
# ==========================================
# Credentials for mock admin login (removed when API is integrated)
# Should match the ADMIN_EMAIL and ADMIN_PASSWORD from the backend
VITE_ADMIN_EMAIL=admin@example.com
VITE_ADMIN_PASSWORD=ChangeThisPassword123

# ==========================================
# API Configuration
# ==========================================
# Backend API URL
VITE_API_URL=http://localhost:8000

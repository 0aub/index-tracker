# ==========================================
# Environment Variables for Raqib Platform
# ==========================================
# Copy this file to .env and fill in the actual values

# ==========================================
# Database Configuration
# ==========================================
POSTGRES_USER=your_db_username
POSTGRES_PASSWORD=your_db_password_here
POSTGRES_DB=your_database_name

# ==========================================
# JWT Authentication
# ==========================================
# IMPORTANT: Generate a strong secret key for production
# You can generate one using: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production-please-use-env-variable

# ==========================================
# Admin User Credentials
# ==========================================
# Default admin account created on first run
ADMIN_EMAIL=admin@example.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeThisPassword123

# Test users password (for seed_users.py script)
TEST_USER_PASSWORD=your-test-password

# ==========================================
# Email / SMTP Configuration
# ==========================================
# SMTP server settings (examples: smtp.gmail.com, smtp.office365.com, smtp.sendgrid.net)
SMTP_HOST=your-smtp-server.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=Your App Name

# ==========================================
# Frontend URL
# ==========================================
# URL where the frontend is hosted
# Used in emails for login links
FRONTEND_URL=http://localhost:8080

# ==========================================
# Frontend Mock Password (Development Only)
# ==========================================
# Used for mock authentication in the UI before API integration
# Should match ADMIN_PASSWORD for consistency
VITE_ADMIN_PASSWORD=ChangeThisPassword123

# ==========================================
# Notes
# ==========================================
# - Never commit the actual .env file to version control
# - Keep your SECRET_KEY and SMTP_PASSWORD secure
# - For production, use environment-specific values
# - SMTP_HOST, SMTP_PORT, and other defaults are set in docker-compose.yml
